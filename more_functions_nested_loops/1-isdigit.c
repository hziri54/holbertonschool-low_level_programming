#include "main.h"

/**
 * _isupper - entry point
 *
 * @c: letter
 *
 * Description: checks for a digit 0 to 9
 *
 * Return: 0 for success.
 */

int _isdigit(int c);
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	} {
		return (0);
	}
}
